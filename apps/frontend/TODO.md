# White Theme Redesign TODO

## Global / Shared

- [x] `index.css` — body `bg-white`, glass `bg-white/90` + `border-slate-200/80`
- [x] `App.tsx` — `bg-white` + 404 text fixed for light bg
- [x] `Navigation.tsx` — white navbar with slate text, bottom border, white backdrop

## Pages

- [x] `Home.tsx` — rewritten with white bg, slate-900 text, fixed broken refs (NavCard/health)
- [x] `Chat.tsx` — verified correct (already light-compatible)
- [x] `Kiosk.tsx` — white page bg, slate-900 text, light cards
- [x] `Student.tsx` — white bg, slate text, light cards
- [x] `TeacherDashboard.tsx` — white bg, slate-900 headings, slate-600 body, light cards
- [x] `TeacherLogin.tsx` — `bg-white`
- [x] `Simulation.tsx` — white bg, slate-900 text, fixed white-on-white text

## Components

- [x] `Hero.tsx` — already light-compatible
- [x] `FeatureCard.tsx` — already light-compatible
- [x] `Card.tsx` — already white cards

## Verify

- [x] Final check - no white-on-white text issues remaining
- [x] All intentional dark elements are overlays on images or isolated buttons on white bg
- [x] Complete
